---
title: tx sync
description: Git-backed synchronization via append-only stream event logs
---

import { Tabs, Tab } from 'fumadocs-ui/components/tabs'

## Purpose

`tx sync` keeps local state portable across machines by writing append-only event logs under `.tx/streams/` and replaying them into SQLite.

This gives you:

- Git-friendly sync artifacts
- Cross-machine collaboration
- Deterministic rebuilds from event history
- Local-first operation (SQLite remains the runtime store)

## At A Glance

<Mermaid
  title="Cross-Machine Sync"
  caption="Export writes the local event log. Git moves the log. Import materializes unseen events on the other machine."
  chart={`sequenceDiagram
    autonumber
    participant A as Machine A
    participant G as Git
    participant B as Machine B

    A->>A: mutate task state
    A->>A: tx sync export
    A->>G: git push .tx/stream.json + .tx/streams/**
    G->>B: git pull
    B->>B: tx sync import
    B->>B: apply unseen events to SQLite
  `}
/>

<Mermaid
  title="Rebuild Path"
  caption="Hydrate replays stream logs into SQLite. Status tells you whether the materialized state is healthy and dirty."
  chart={`flowchart LR
    Operator[Operator] --> Hydrate[tx sync hydrate]
    Logs[.tx/streams/**/*.jsonl] --> Hydrate
    Hydrate --> Sqlite[Rebuilt SQLite state]
    Sqlite --> Status[tx sync status]
    Status --> Checks[counts, dirty flag, last import/export]
  `}
/>

## Sync Model (Default)

`tx sync` uses a stream event log model:

```text
.tx/
├── stream.json                              # Local stream identity
└── streams/
    └── <stream-id>/
        └── events-YYYY-MM-DD.jsonl          # Append-only events
```

Each event is a JSON object with envelope metadata and payload:

```json
{
  "event_id": "01JZ...",
  "stream_id": "01JY...",
  "seq": 184,
  "ts": "2026-03-05T13:12:04.123Z",
  "type": "task.upsert",
  "entity_id": "tx-abc123",
  "v": 2,
  "payload": { "id": "tx-abc123", "title": "Implement auth" }
}
```

## Subcommands

| Command | Description |
|---------|-------------|
| `tx sync export` | Emit current state as stream events |
| `tx sync import` | Incrementally apply stream events |
| `tx sync stream` | Show local stream ID + sequence state |
| `tx sync hydrate` | Rebuild materialized state from event logs |
| `tx sync status` | Show sync health and dirty status |
| `tx sync auto` | Enable/disable automatic export on mutations |

## tx sync export

Write events to the local stream log.

<Tabs groupId="interface" persist items={["CLI", "TypeScript SDK", "MCP", "REST API"]}>
<Tab value="CLI">

```bash
tx sync export [--json]
```

```bash
# Export events
tx sync export

# JSON output
tx sync export --json
```

CLI output (human mode):

```text
Events: 42 event(s) → .tx/streams/<stream-id>/events-2026-03-05.jsonl
```

</Tab>
<Tab value="TypeScript SDK">

```typescript
import { TxClient } from '@jamesaphoenix/tx-agent-sdk'

const tx = new TxClient({ dbPath: '.tx/tasks.db' })

const result = await tx.sync.export()
console.log(result.eventCount, result.streamId, result.path)
```

</Tab>
<Tab value="MCP">

**Tool:** `tx_sync_export`

```json
{
  "name": "tx_sync_export",
  "arguments": {}
}
```

</Tab>
<Tab value="REST API">

```text
POST /api/sync/export
```

```bash
curl -X POST http://localhost:3456/api/sync/export \
  -H "Content-Type: application/json" \
  -d '{}'
```

Response:

```json
{
  "eventCount": 42,
  "streamId": "01JY...",
  "path": ".tx/streams/01JY.../events-2026-03-05.jsonl"
}
```

</Tab>
</Tabs>

## tx sync import

Read stream event logs and apply unseen events.

<Tabs groupId="interface" persist items={["CLI", "TypeScript SDK", "MCP", "REST API"]}>
<Tab value="CLI">

```bash
tx sync import [--json]
```

```bash
tx sync import
tx sync import --json
```

CLI output (human mode):

```text
Events: imported=42, applied=42, streams=1
```

</Tab>
<Tab value="TypeScript SDK">

```typescript
const result = await tx.sync.import()
console.log(result.importedEvents, result.appliedEvents, result.streamCount)
```

</Tab>
<Tab value="MCP">

**Tool:** `tx_sync_import`

```json
{
  "name": "tx_sync_import",
  "arguments": {}
}
```

</Tab>
<Tab value="REST API">

```text
POST /api/sync/import
```

```bash
curl -X POST http://localhost:3456/api/sync/import \
  -H "Content-Type: application/json" \
  -d '{}'
```

Response:

```json
{
  "importedEvents": 42,
  "appliedEvents": 42,
  "streamCount": 1
}
```

</Tab>
</Tabs>

## tx sync stream

Inspect local stream identity and sequence progress.

<Tabs groupId="interface" persist items={["CLI", "TypeScript SDK", "MCP", "REST API"]}>
<Tab value="CLI">

```bash
tx sync stream [--json]
```

```text
Stream: 01JY...
  nextSeq: 185
  lastSeq: 184
  eventsDir: /.../.tx/streams/01JY...
```

</Tab>
<Tab value="TypeScript SDK">

```typescript
const stream = await tx.sync.stream()
console.log(stream.streamId, stream.nextSeq, stream.lastSeq, stream.eventsDir)
```

</Tab>
<Tab value="MCP">

**Tool:** `tx_sync_stream`

```json
{
  "name": "tx_sync_stream",
  "arguments": {}
}
```

</Tab>
<Tab value="REST API">

```text
GET /api/sync/stream
```

```bash
curl http://localhost:3456/api/sync/stream
```

</Tab>
</Tabs>

## tx sync hydrate

Clear and rebuild materialized task state by replaying event logs.

<Tabs groupId="interface" persist items={["CLI", "TypeScript SDK", "MCP", "REST API"]}>
<Tab value="CLI">

```bash
tx sync hydrate [--json]
```

</Tab>
<Tab value="TypeScript SDK">

```typescript
const hydrated = await tx.sync.hydrate()
console.log(hydrated.importedEvents, hydrated.appliedEvents, hydrated.rebuilt)
```

</Tab>
<Tab value="MCP">

**Tool:** `tx_sync_hydrate`

```json
{
  "name": "tx_sync_hydrate",
  "arguments": {}
}
```

</Tab>
<Tab value="REST API">

```text
POST /api/sync/hydrate
```

```bash
curl -X POST http://localhost:3456/api/sync/hydrate
```

</Tab>
</Tabs>

## tx sync status

Show high-level sync state.

<Tabs groupId="interface" persist items={["CLI", "TypeScript SDK", "MCP", "REST API"]}>
<Tab value="CLI">

```bash
tx sync status [--json]
```

```text
Sync Status:
  Tasks in database: 156
  Events in stream logs: 312
  Last export: 2026-03-05T12:40:33.000Z
  Last import: 2026-03-05T12:41:18.000Z
  Dirty (unexported changes): no
  Auto-sync: enabled
```

</Tab>
<Tab value="TypeScript SDK">

```typescript
const status = await tx.sync.status()
console.log(status.dbTaskCount, status.isDirty, status.autoSyncEnabled)
```

</Tab>
<Tab value="MCP">

**Tool:** `tx_sync_status`

```json
{
  "name": "tx_sync_status",
  "arguments": {}
}
```

</Tab>
<Tab value="REST API">

```text
GET /api/sync/status
```

```bash
curl http://localhost:3456/api/sync/status
```

</Tab>
</Tabs>

## tx sync auto

Enable or disable automatic export on mutations.

```bash
tx sync auto --enable
tx sync auto --disable
tx sync auto --json
```

When enabled, mutating operations trigger sync export automatically.

## Sync Constraints

- Legacy single-file sync arguments are no longer supported.

## Git Workflow

```bash
# Before commit
tx sync export
git add .tx/stream.json .tx/streams

git commit -m "chore: sync stream events"
git push

# On another machine
git pull
tx sync import
```

## .gitignore Recommendation

```text
# Keep database local
.tx/tasks.db
.tx/tasks.db-wal
.tx/tasks.db-shm

# Track sync stream identity + event logs
!.tx/stream.json
!.tx/streams/
!.tx/streams/**
```

## Related Commands

- [`tx ready`](/docs/primitives/ready) - Pull the next workable task after import
- [`tx done`](/docs/primitives/done) - Mark work complete (can trigger auto-sync)
- [`tx pin`](/docs/primitives/pin) - Sync context files alongside task/event sync
